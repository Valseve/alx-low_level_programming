#include "main.h"

/**
 * _abs - Entry point
 *
 * @int: The integer to check
 * Return: the absolute value of int
 */
int _abs(int)
{
	int i, j;

	i = 0;
	j = 1
	while (i <= '0')
	{
		return (i);
		while (j = i * -1)
		{
			return (j);
		}
	}
}
